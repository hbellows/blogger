<h1>All Articles</h1>

<% @articles.each do |article| %>
  <ul>
    <li><%= link_to article.title, articles_path(article), class: 'article-title' %></li>
    <li><%= article.body %></li>
    <% end %>
  </ul>

<br><br />
  <%= link_to "Create a New Article", new_article_path, class: "new-article" %>
